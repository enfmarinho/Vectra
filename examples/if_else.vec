import println

func main() -> int:
    int x = 80
    string msg
    if x > 80:
	    msg = "x maior que 80"
    else if x > 40:
	    msg = "x maior que 40"
    else:
	    msg = "x menor que 40"

    println(msg)
